# v2.4.3 — JankenSQLHub version fix

## JankenSQLHub Version Update

Fixed a bug by updating JankenSQLHub dependency from version 1.3 to 1.4.0, ensuring compatibility and proper functionality of all SQL query operations.

## Installation

```bash
curl -fsSL https://raw.githubusercontent.com/pandazy/jankenoboe/main/install.sh | sh
```

## Platform Support

Pre-built binaries for:
- Linux x86_64
- Linux ARM64 (aarch64)
- macOS Apple Silicon (aarch64)